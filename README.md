# CodeToAGI — Episode 26: Context Managers

**Python Context Managers | with, __enter__, __exit__, contextlib, ExitStack**

## 📁 Files

- `manim_ep26.py` — Manim animation script
- `generate_ep26.py` — Full video generation script
- `challenge_solution.py` — **Complete solution to the viewer challenge**

## 🎯 Challenge Solution

The file `challenge_solution.py` contains clean, well-commented solutions for all challenge steps:

1. **Class-based `Suppressor`**
2. **Generator-based `@contextmanager` version**
3. **`Timer` context manager** with performance measurement
4. **Dynamic file handling with `ExitStack`**
5. **Combined usage** of multiple context managers

### How to Run

```bash
python challenge_solution.py
